<h1><%= @student.to_s %></h1>

<div>
  <% if @student.active  %>
    This student is currently active.
  <% else %>
    This student is currently inactive.
  <% end %>
</div>

<div>
  <%= link_to "Toggle Activation", activate_student_path(@student) %>
</div>
