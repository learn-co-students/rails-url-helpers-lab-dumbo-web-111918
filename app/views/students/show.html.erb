<h1><%= @student.to_s %></h1>

<h2>This student is currently <%= @student.active ? "active.":"inactive." %></h2>

<p><a href="<%=activate_student_path(@student)%>"><%=@student.active ? "Deactivate":"Activate"%></p>
